# from django import template

# register = template.Library()

# @register.filter
# def multiply(value, arg):
#     try:
#         return round(float(value) * float(arg), 2)  # Ensures both are treated as floats
#     except (ValueError, TypeError):
#         return 0  # Returns 0 in case of any error
